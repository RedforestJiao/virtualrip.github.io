.fa-bilibili {
    background: url(/images/bilibili.svg);
    background-size: 1em 1em;
    opacity: 0.55;
    background-position: 0.05rem 0.2rem;
    background-repeat: no-repeat;
    height: 1.1rem;
    width: 1rem;
    border-radius: 0rem;
    /*鼠标停留在图标上时，图标呈现发光效果*/
    &:hover {
        opacity: 1;
}
}

// Custom styles.
// 整体背景设置
body {
 	background:url(/images/IMG_0097.JPG);// 设定背景图片,images同处于source文件夹下
 	background-repeat: no-repeat;// 设定背景图片非重复填充
    background-attachment:fixed;// 设置背景图片不随页面滚动
    background-position:50% 50%;// 设置背景图片位置
	background-size: cover// 设置保持图像的纵横比并将图像缩放成将完全覆盖背景定位区域的最小大小
}

/* 添加遮罩层 */
body::before {
  content: "";
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(255, 255, 255, 0.4); /* 白色半透明蒙版 */
  z-index: -1; /* 放到最底层 */
}

//超链接颜色调整
a:not(.btn){
  //color: #0000FF; //超链接显示颜色
  border-bottom: none;
  &:hover {
	color:rgb(211, 136, 16);  //鼠标移动上去后超链接颜色
	font-weight: bold;
	text-decoration: underline;
	}
  }

// 文章背景的透明度
.content-wrap {
  opacity: 0.8;
}

//菜单字体颜色
a, span.exturl {
    color:rgb(255, 255, 255) ;
}

//标题颜色
.brand {
  border-bottom: none;
  color:rgb(255, 255, 255) !important;
  display: inline-block;
  line-height: $font-size-title;
  padding: 40px 0px;
  position: center;

  &:hover {
    color: rgb(0, 0, 255) !important; //悬停颜色，被链接颜色覆盖
  }
}
.site-subtitle {
  color: rgb(255, 255, 255);
  font-size: $font-size-subtitle;
  margin: 10px 0;
}

// 页面头样式属性
.header-inner {
  // 也可以同时定义背景色
   background: #ddd;
  // 透明度
   opacity: 0.8;
}

// sidebar侧边工具栏样式属性
.sidebar{
    // 动画过渡时间
	transition-duration: 0.6s;
    // 透明度
	opacity: 0.8;
    //单独设置字体颜色
    p,span,a {color:rgb(22, 3, 5) !important;}

    //background:url();
    //background-size: cover;
    //background-position:center;
    //background-repeat:no-repeat;
    //z-iNdex:0
}

//头像栏背景
.sidebar-inner {
  background: rgba(255, 255, 255, 0.64) none repeat scroll !important; //调节侧栏背景透明度，不覆盖到字体，头像等
}
//菜单栏背景
.header-inner {
  background: rgba(9, 200, 243, 0.8);
}
//标题栏背景
.site-brand-container{
  background: rgba(71, 210, 237, 0.64); //调节标题栏透明度
  padding: 40px 0px;
}


  
// 标题样式
.posts-expand .post-title-link {
	// 设置字体颜色
	color: #222;
}



// 文章版块样式
.post-block {
    //background: var(--content-bg-color);
	background: #fff
    border-radius: initial;
    box-shadow: 0 2px 2px 0 rgba(0,0,0,0.12), 0 3px 1px -2px rgba(0,0,0,0.06), 0 1px 5px 0 rgba(0,0,0,0.12);
    padding: 50px;
}


.post {
   margin-top: 60px;
   margin-bottom: 60px;
   padding: 25px;
   -webkit-box-shadow: 0 0 5px rgba(251, 251, 251, 0.49);
   -moz-box-shadow: 0 0 5px rgba(236, 236, 236, 0.47);
  }

/* 修改分类页中的分类链接颜色 */
.category-list-link {
  color: #ff6b81;     /* 普通状态颜色 */
}

.category-list-link:hover {
  color: #0000ff !important;    /* 鼠标悬停时颜色 */
}

/* 修改标签页标题“标签”的颜色 */
.page-title {
  color: #ff7b7b;  /* 页面标题文字颜色 */
}

/* 修改标签列表中的每个标签链接颜色 */
.tag-cloud a {
  color: #4dabf7 !important;          /* 正常状态 */
  transition: color 0.3s;  /* 平滑过渡 */
}

.tag-cloud a:hover {
  color: #ff6b81;          /* 鼠标悬停颜色 */
}

/* 修改标签计数数字颜色（如果有） */
.tag-cloud a .tag-count {
  color: #aaa;
}

// 底部字体颜色
.footer-inner {color:rgb(130, 213, 143);}


